export const metadata = {
  title: "Conversation",
  description:
    "Share one phone and interpret naturally between English and Kinyarwanda with Kasuku.",
};

export default function ConversationLayout({ children }) {
  return children;
}
